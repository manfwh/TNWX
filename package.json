{
  "private": true,
  "scripts": {
    "build": "pnpm -r tsc",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "tsc": "lerna run tsc --stream",
    "tsc:watch": "lerna run watch --stream",
    "pub": "lerna publish --skip-git",
    "del": "lerna clean",
    "build:miniprogram": "lerna run tsc --scope=@tnwx/miniprogram"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^20.11.3",
    "jest": "^24.9.0",
    "lerna": "^8.0.2",
    "prettier": "^1.18.2",
    "ts-jest": "^24.2.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "nx": "^17.2.8"
  }
}
